# Security Policy

Do not open a public issue containing iAquaLink credentials, access tokens, cookies, system serial numbers, raw API responses, Homebridge PINs, or Matter setup codes.

The plugin must redact sensitive fields before logging or generating diagnostics. Authentication secrets must remain only in the user's Homebridge configuration and memory needed for the active session.
