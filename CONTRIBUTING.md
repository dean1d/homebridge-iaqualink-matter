# Contributing

Use Node.js 22.12 or newer. Run `npm install`, then `npm run check` before opening a pull request. Never commit credentials or raw iAquaLink payloads. New behavior should include tests and a CHANGELOG entry.
